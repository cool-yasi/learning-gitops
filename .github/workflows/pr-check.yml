name: PR Check

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Say who triggers it
        run: echo "PR was opened!"
      - name: List files
        run: ls -la
